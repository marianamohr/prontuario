# Database (Railway fornece DATABASE_URL)
DATABASE_URL=postgres://prontuario:prontuario_secret@localhost:5432/prontuario?sslmode=disable

# JWT (gerar: openssl rand -base64 32)
JWT_SECRET=your-jwt-secret-min-32-chars

# Criptografia em repouso (gerar: openssl rand -base64 32 para cada versão)
# Formato: v1:base64key,v2:base64key
DATA_ENCRYPTION_KEYS=v1:your-base64-encryption-key-32-bytes
CURRENT_DATA_KEY_VERSION=v1

# SMTP (obrigatório para "Enviar contrato para assinar", convites e reset de senha)
# - Desenvolvimento com Docker: use MailHog (já no docker-compose). E-mails NÃO vão para sua caixa de entrada;
#   veja os e-mails em http://localhost:8025 (Web UI do MailHog).
# - Para receber no e-mail de verdade: configure um servidor SMTP real (ex.: Gmail, SendGrid).
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USER=
SMTP_PASS=
SMTP_FROM_NAME=Prontuário Saúde
SMTP_FROM_EMAIL=noreply@localhost
# Exemplo Gmail (use "Senha de app" da conta Google): SMTP_HOST=smtp.gmail.com, SMTP_PORT=587, SMTP_USER=seu@gmail.com, SMTP_PASS=xxxx
# Exemplo SendGrid: use o host e a API Key como SMTP_PASS conforme a documentação.

# URLs públicas (usadas nos links dos e-mails)
# IMPORTANTE: APP_PUBLIC_URL deve ser a URL onde o FRONTEND abre no navegador.
# - Desenvolvimento (npm run dev): use http://localhost:5173 (porta do Vite).
# - Docker (frontend na porta 80): use http://localhost ou http://localhost:80.
APP_PUBLIC_URL=http://localhost:5173
BACKEND_PUBLIC_URL=http://localhost:8080

# Backend
PORT=8080

# CORS (origins permitidos, separados por vírgula)
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# WhatsApp (Twilio) – lembretes de consulta. Se vazios, lembretes não são enviados.
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_WHATSAPP_FROM=whatsapp:+14155238886
# Fuso para calcular "amanhã" no job de lembrete (cron). Padrão: America/Sao_Paulo
REMINDER_CRON_TZ=America/Sao_Paulo
